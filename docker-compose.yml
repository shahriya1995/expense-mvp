services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: expense-mvp-mcp:latest
    container_name: expense-mvp-mcp
    ports:
      - "4000:4000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
