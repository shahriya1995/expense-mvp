services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: expense-mvp-api:latest
    container_name: expense-mvp-api
    ports:
      - "4000:4000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
